RapidGame upgrade instructions:

* Check in existing edits to RapidGame.
* Move old cocos2d-js folder out.
* Copy new cocos2d-js folder in.
* Remove samples, docs, templates, tools.
* ???? find src/cocos2d-x -name ".gitmodules" -delete
* Git add --all and commit.
* Apply existing cocos2d.patch manually to new cocos2d-js folder.
* Check in.
* Upgrade package.json version and cocos2djs URL in rapidgame.js AND src/downloaded.txt.
* Check in.
* Verify rapidgame prebuild (sudo npm unlink rapidgame -g; cd ~/code/RapidGame; sudo npm link .).
* Verify TwoScene iOS, Mac, Android.
* Check in any tweaks.
* Build new patch file (see bottom of rapidgame.js).
* Check in new patch file.
* make rg && cd ../releases/RapidGame && sudo npm uninstall rapidgame -g; sudo npm uninstall rapidgame -g; sudo npm install . -g && rapidgame prebuild
* Test on Windows
* Update Changelog.
* Check in any final edits to RapidGame and make rg.
* Commit and push new RapidGame, create release on Github.
* Publish RapidGame using sudo npm publish .
* Update docs (na rez/docco/docco.jst && scp -r ../releases/RapidGame/docs/* ssl.wizardfu.com:w/wizardfu.com/docs/rapidgame/)
* Update SpriteDraggerX
